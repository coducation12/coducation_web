# Coducation 아키텍처 문서

## 🏗️ 시스템 아키텍처

### 전체 구조

```
┌─────────────────────────────────────────────────────────┐
│                    Client (Browser)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Student    │  │   Parent     │  │   Teacher    │  │
│  │   Dashboard  │  │   Dashboard  │  │   Dashboard  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Next.js 15 (App Router)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Server       │  │ Server       │  │ Client       │  │
│  │ Actions      │  │ Components   │  │ Components   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    Supabase                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ PostgreSQL   │  │   Auth       │  │   Storage    │  │
│  │ Database     │  │   Service    │  │   Service    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Google AI (Genkit)                          │
│              (타자 연습 문제 생성)                        │
└─────────────────────────────────────────────────────────┘
```

## 🔐 인증 아키텍처

### 하이브리드 인증 시스템

프로젝트는 역할에 따라 다른 인증 방식을 사용합니다:

#### 1. 학생/학부모 인증 (Custom Auth)
- **방식**: 데이터베이스 직접 인증
- **저장 위치**: `users` 테이블
- **세션 관리**: HTTP 쿠키
- **비밀번호**: bcrypt 해싱

```typescript
// 인증 흐름
로그인 요청 → DB에서 사용자 조회 → 비밀번호 검증 → 쿠키 설정
```

#### 2. 강사/관리자 인증 (Supabase Auth)
- **방식**: Supabase Authentication
- **저장 위치**: Supabase Auth 서비스
- **세션 관리**: Supabase Session + HTTP 쿠키
- **비밀번호**: Supabase Auth 관리

```typescript
// 인증 흐름
로그인 요청 → Supabase Auth 인증 → 토큰 발급 → 쿠키 설정
```

### 인증 미들웨어

```typescript
// middleware.ts에서 역할별 라우트 보호
- /dashboard/student/* → student 역할만 접근
- /dashboard/parent/* → parent 역할만 접근
- /dashboard/teacher/* → teacher 역할만 접근
- /dashboard/admin/* → admin 역할만 접근
```

## 📊 데이터베이스 아키텍처

### 주요 테이블 구조

1. **users**: 모든 사용자 정보
2. **students**: 학생별 상세 정보
3. **teachers**: 강사별 상세 정보
4. **curriculums**: 교육 과정 정보
5. **main_curriculums**: 메인 페이지 표시용 커리큘럼
6. **typing_exercises**: 타자 연습 문제
7. **student_activity_logs**: 학생 활동 기록
8. **community_posts**: 커뮤니티 게시글
9. **community_comments**: 커뮤니티 댓글
10. **consultations**: 상담 문의

### RLS (Row Level Security)

각 테이블에 역할별 접근 정책이 설정되어 있습니다:

- **학생**: 자신의 데이터만 접근
- **학부모**: 자녀의 데이터만 접근
- **강사**: 담당 학생의 데이터만 접근
- **관리자**: 모든 데이터 접근 가능

## 🎨 프론트엔드 아키텍처

### 컴포넌트 구조

```
components/
├── common/              # 공통 컴포넌트
│   ├── header.tsx
│   ├── footer.tsx
│   ├── sidebar.tsx
│   └── layout-wrapper.tsx
├── ui/                  # shadcn/ui 컴포넌트
│   ├── button.tsx
│   ├── card.tsx
│   ├── dialog.tsx
│   └── ...
├── landing/             # 랜딩 페이지 컴포넌트
│   ├── hero-section.tsx
│   ├── academy-section.tsx
│   ├── curriculum-section.tsx
│   └── instructors-section.tsx
├── curriculum/          # 커리큘럼 관련
│   ├── curriculum-card.tsx
│   ├── curriculum-upload.tsx
│   └── curriculum-memo.tsx
└── community/           # 커뮤니티 관련
    ├── PostCard.tsx
    ├── CommentCard.tsx
    └── PostForm.tsx
```

### 상태 관리

- **서버 상태**: Next.js Server Actions를 통한 데이터 페칭
- **클라이언트 상태**: React Hooks (useState, useEffect)
- **폼 상태**: react-hook-form + zod 검증

### 스타일링 시스템

- **TailwindCSS**: 유틸리티 기반 스타일링
- **CSS Variables**: 다크 모드 및 테마 관리
- **shadcn/ui**: 재사용 가능한 UI 컴포넌트

## 🔄 데이터 흐름

### 1. 데이터 읽기 (Read)

```
Client Component
    ↓
Server Component / Server Action
    ↓
Supabase Client
    ↓
PostgreSQL Database
    ↓
RLS 정책 검증
    ↓
데이터 반환
```

### 2. 데이터 쓰기 (Write)

```
Client Component
    ↓
Form Submit
    ↓
Server Action
    ↓
Supabase Client
    ↓
PostgreSQL Database
    ↓
RLS 정책 검증
    ↓
데이터 저장
    ↓
응답 반환
```

### 3. 이미지 업로드

```
Client Component
    ↓
이미지 선택
    ↓
Canvas API로 압축 (WebP)
    ↓
Supabase Storage 업로드
    ↓
Public URL 반환
    ↓
데이터베이스에 URL 저장
```

## 🤖 AI 통합

### Google AI (Genkit) 활용

- **용도**: 타자 연습 문제 동적 생성
- **위치**: `src/ai/flows/suggest-typing-exercise.ts`
- **프로세스**:
  1. 학생의 과거 타자 연습 결과 분석
  2. 현재 커리큘럼 정보 확인
  3. AI에게 맞춤형 문제 생성 요청
  4. 생성된 문제를 데이터베이스에 저장

## 📦 의존성 관리

### 주요 패키지

- **Next.js 15.3.3**: React 프레임워크
- **@supabase/supabase-js 2.51.0**: Supabase 클라이언트
- **@genkit-ai/googleai 1.13.0**: Google AI 통합
- **react-hook-form 7.54.2**: 폼 관리
- **zod 3.24.2**: 스키마 검증
- **tailwindcss 3.4.1**: 스타일링
- **lucide-react 0.475.0**: 아이콘

## 🚀 배포 아키텍처

### 배포 환경

- **프로덕션**: Vercel / Firebase App Hosting
- **데이터베이스**: Supabase (PostgreSQL)
- **스토리지**: Supabase Storage
- **CDN**: Vercel Edge Network / Firebase CDN

### 빌드 프로세스

```
Source Code
    ↓
TypeScript 컴파일
    ↓
Next.js 빌드
    ↓
정적 파일 생성
    ↓
배포 플랫폼 업로드
    ↓
CDN 배포
```

## 🔒 보안 아키텍처

### 보안 계층

1. **네트워크 레벨**: HTTPS 강제
2. **애플리케이션 레벨**: 역할 기반 접근 제어
3. **데이터베이스 레벨**: RLS 정책
4. **인증 레벨**: 하이브리드 인증 시스템

### 환경 변수 관리

- **클라이언트**: `NEXT_PUBLIC_*` 접두사
- **서버**: `SUPABASE_SERVICE_ROLE_KEY` 등
- **저장 위치**: `.env.local` (로컬), 배포 플랫폼 환경 변수 (프로덕션)

## 📈 성능 최적화

### 이미지 최적화
- WebP 형식으로 자동 변환
- 자동 리사이징
- 프로그레시브 로딩

### 코드 최적화
- Next.js 자동 코드 스플리팅
- 동적 임포트
- 서버 컴포넌트 활용

### 캐싱 전략
- Supabase Storage: 1년 캐시 설정
- Next.js: 정적 페이지 자동 캐싱

